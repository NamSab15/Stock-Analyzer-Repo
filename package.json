{
  "name": "indian-stocks-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "mongod": "mongod --dbpath C:/data/db",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "start": "npm-run-all --parallel mongod backend frontend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
